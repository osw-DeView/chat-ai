# ==============================================================================
# [Core Application Framework]
# FastAPI 서버 실행을 위한 핵심 라이브러리
# ==============================================================================
fastapi==0.121.1
uvicorn==0.38.0
pydantic==2.12.4

# ==============================================================================
# [HTTP Communication]
# inference.py에서 독립 실행된 llama-server API와 통신하기 위한 HTTP 클라이언트
# ==============================================================================
httpx==0.28.1

# ==============================================================================
# [Utilities]
# 코드 내에서 사용되는 유틸리티 라이브러리
# ==============================================================================
# inference.py에서 LLM의 응답(마크다운)을 일반 텍스트로 파싱하는 데 사용
markdown-it-py==4.0.0

# FastAPI가 form-data를 처리하기 위해 필요할 수 있음 (최소 의존성으로 포함)
python-multipart==0.0.20