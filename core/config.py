# core/config.py

# --- 💡 1. 모델 경로 설정 ---
# 이 부분은 자신의 학습 환경 및 모델 경로에 맞게 수정해야 합니다.
GGUF_MODEL_PATH = "./qwen3-8b-cs-interviewer-merge-v1-150-Q4_K_M.gguf"

# --- 💡 2. 프롬프트 템플릿 설정 ---
# 이 프롬프트들은 학습 시 사용했던 형식과 정확히 일치해야 합니다.

# [수정] 꼬리질문, 최종 평가 등 모든 상황에서 모델의 역할을 정의하는 범용 시스템 프롬프트
SYSTEM_PROMPT = "당신은 최고의 IT 기술 면접 전문가입니다. 당신은 지원자와 심층적인 기술 대화를 나눌 수 있으며, 면접이 끝난 후 '# [지시]' 태그를 포함한 명확한 '평가' 지시를 받으면, 전체 대화 기록을 분석하여 상세한 최종 평가를 생성할 수 있습니다."

# --- 💡 3. 생성 파라미터 설정 ---
# 추론 시 모델의 답변 스타일을 조절하는 하이퍼파라미터입니다.
GENERATION_CONFIG = {
    "max_new_tokens": 1024,
    "do_sample": True,
    "temperature": 0.1,
    "top_p": 0.95,
}